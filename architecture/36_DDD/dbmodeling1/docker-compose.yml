version: '3.9'
services:
  prisma-db:
    container_name: prisma-db
    image: postgres:12.4
    ports:
      - '5403:5432'
    volumes:
      - prisma-store:/var/lib/postgres/data
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: prisma
      POSTGRES_INITDB_ARGS: '--encoding=UTF-8'
      TZ: 'Asia/Tokyo'
  prisma-test-db:
    container_name: prisma-test-db
    image: postgres:12.4
    ports:
      - '5402:5432'
    volumes:
      - prisma-test-store:/var/lib/postgres/data
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: prisma
      POSTGRES_INITDB_ARGS: '--encoding=UTF-8'
      TZ: 'Asia/Tokyo'

volumes:
  prisma-store:
    driver: local
  prisma-test-store:
    driver: local
